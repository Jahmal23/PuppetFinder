const mailer = require('../helpers/mailer');
var expect = require('chai').expect;

describe('#Mailer', function() {
    it('should not blow up', function() {
      expect(true).to.equal(true);
      //assert.equal(false, mailer === null);
    });
  });
